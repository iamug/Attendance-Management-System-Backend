"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Transform slash notation to the controller's method, appending/prepending any middleware required
 *
 * @export
 * @param {any} ctrllr
 * @returns
 */
const box_1 = __importDefault(require("../Container/box"));
function default_1(ctrllr) {
    const [controllerName, methodName] = ctrllr.split("@");
    if (controllerName.includes("/")) {
        let controllerPath = controllerName.split("/");
        let controllerClass = box_1.default["Ioc"].use(controllerPath[controllerPath.length - 1]);
        return controllerClass[methodName];
    }
    let controllerClass = box_1.default["Ioc"].use(controllerName);
    return controllerClass[methodName];
}
exports.default = default_1;
