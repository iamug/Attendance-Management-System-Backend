"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
//@ts-ignore
const app_1 = __importDefault(require("../../../../Config/app"));
function use(namespace) {
    let data;
    if (app_1.default.default.aliases[namespace]) {
        try {
            data = require(app_1.default.default.aliases[namespace]);
        }
        catch (error) {
            throw error;
        }
    }
    else {
        data = require(namespace);
    }
    try {
        let resolver = data.default;
        return new resolver();
    }
    catch (error) {
        return data;
    }
}
const _global = global;
_global.use = use;
