declare class AppShutDown {
    /**
     * This check if App is down for maintenance
     * @return Array
     */
    static isDownForMaintenance(): {
        status: boolean;
        message: string;
        retry: number | null;
        endPoints: any;
    };
    /**
     * Determine if the request has a URI that should be accessible in maintenance mode
     * @param {Request} request
     * @param {Object} endpoints
     * @return Boolean
     */
    static inEndpointsArray(request: string, endpoints: any): any;
    private static maintanceMode;
    /**
     * Get the payload to be sent to the client
     */
    private static checkMode;
    private static getMessage;
    /**
     * Get the number of seconds the client should wait before retrying their request.
     * @param {Number | null} retry
     */
    private static getRetryTime;
}
export default AppShutDown;
//# sourceMappingURL=shutdown.d.ts.map