"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const argon2_1 = __importDefault(require("argon2"));
class ArgonHasher {
    /**
     * Create a new hasher instance.
     *
     * @param  array  $options
     * @return void
     */
    constructor(config) {
        /**
         * The default cost factor.
         *
         * @var int
         */
        this.hashLength = 32;
        if (config.hashLength)
            this.hashLength = config.hashLength;
        this.config = config;
    }
    /**
     * Hash the given value.
     *
     * @param  string  value
     * @return string
     *
     * @throws \RuntimeException
     */
    async make(value) {
        try {
            let hash = await argon2_1.default.hash(value, this.config);
            return hash;
        }
        catch (error) {
            throw error;
        }
    }
    /**
     * Check the given plain value against a hash.
     *
     * @param  string  value
     * @param  string  hashedValue
     * @return bool
     *
     * @throws \RuntimeException
     */
    async check(value, hashedValue) {
        try {
            let password = await argon2_1.default.verify(hashedValue, value);
            return password;
        }
        catch (error) {
            throw error;
        }
    }
}
exports.default = ArgonHasher;
