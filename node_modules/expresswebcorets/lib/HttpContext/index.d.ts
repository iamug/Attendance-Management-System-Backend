/// <reference types="node" />
import { Request as expressRequest, Response, NextFunction } from "express";
import fileUpload from "express-fileupload";
interface Request extends expressRequest {
    validate(data: object, rules: object): object | Promise<object>;
    user: object;
    file: {
        files?: fileUpload.FileArray | undefined;
        fieldName(fieldName: string): {
            name: string;
            data: Buffer;
            size: number;
            encoding: "7bit";
            tempFilePath: "";
            truncated: false;
            mimetype: string;
            md5: string;
        };
        move(filePath: string): Promise<{
            status: boolean;
            message: object | string;
        }>;
        disk(diskType: string): Request["file"];
        put(file: string, diskDirectory?: string | undefined): Promise<{
            status: boolean;
            message: object | string;
        }>;
    };
}
export { Request, Response, NextFunction };
//# sourceMappingURL=index.d.ts.map