declare class Ioc {
    entries: any;
    constructor();
    private isClass;
    private getAllFunction;
    private componentScan;
    register(namespace: string, instructions: any, mode: string): void;
    asProvider(namespace: string, instructions: any, mode: string): void;
    private bootstrapServiceProvider;
    singleton(namespace: string, instructions: any, mode: string): void;
    use(namespace: string): any;
    private resolveBinding;
    private processInstrunction;
    private injectValues;
    private getParamNames;
}
export default Ioc;
//# sourceMappingURL=Ioc.d.ts.map